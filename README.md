# 🤖 BOT-BUSINESS V2.0

**Telegram-бот для записи клиентов** (барбершопы, салоны красоты, мастера услуг)

## ✨ Возможности

### Для клиентов:
- ✅ Запись на услуги с выбором даты и времени
- ✅ Просмотр своих записей
- ✅ Изменение записей (дата/время, услуга)
- ✅ Отмена записей
- ✅ Отправка номера телефона через контакт
- ✅ FAQ и контакты

### Для администраторов:
- ✅ Уведомления о новых записях
- ✅ Уведомления об изменениях и отменах
- ✅ История клиента в каждом уведомлении
- ✅ Админ-панель с статистикой
- ✅ Просмотр всех заказов
- ✅ База клиентов

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонируйте или распакуйте архив
cd bot_business_v2_final

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка

Запустите интерактивный установщик:

```bash
python setup.py
```

Установщик запросит:
- TOKEN бота (от @BotFather)
- Ваш Telegram ID
- Название бизнеса
- Уникальный slug

И автоматически создаст:
- `.env` с токенами
- `configs/client_lite.json` с настройками
- База данных SQLite

### 3. Запуск

```bash
# Основной бот (для клиентов)
python main.py --config configs/client_lite.json

# Админ-бот (опционально)
python admin_bot/main.py --config configs/client_lite.json
```

## 📁 Структура проекта

```
bot_business_v2_final/
├── handlers/              # Обработчики сообщений
│   ├── booking.py        # Создание/редактирование записей
│   └── mybookings.py     # Просмотр/отмена записей
│
├── states/               # FSM состояния
│   └── booking.py       
│
├── utils/                # Утилиты
│   ├── db_manager.py    # Работа с SQLite
│   └── notify.py        # Уведомления админам
│
├── middlewares/          # Middlewares
│   └── config_middleware.py
│
├── admin_bot/            # Админ-панель (отдельный бот)
│   └── main.py
│
├── configs/              # Конфигурации
│   └── client_lite.json # Настройки бизнеса
│
├── main.py              # Точка входа основного бота
├── setup.py             # Интерактивный установщик
└── requirements.txt     # Зависимости
```

## ⚙️ Настройка

### Редактирование услуг

Откройте `configs/client_lite.json`:

```json
{
  "services": [
    {"id": "service1", "name": "Стрижка", "price": 1200},
    {"id": "service2", "name": "Стрижка + борода", "price": 1800}
  ]
}
```

### График работы

```json
{
  "booking": {
    "work_start": 10,      # Начало работы (10:00)
    "work_end": 20,        # Конец работы (20:00)
    "slot_duration": 60    # Длительность слота (60 минут)
  }
}
```

### FAQ

```json
{
  "faq": [
    {"btn": "💰 Цены", "answer": "Наши цены: ..."},
    {"btn": "📍 Адрес", "answer": "г. Москва, ..."},
    {"btn": "🕐 Часы работы", "answer": "Пн-Пт: 10:00-20:00"}
  ]
}
```

## 🔧 Возможности конфигурации

### Функции (включить/выключить)

```json
{
  "features": {
    "enable_slot_booking": true,    # Бронирование по слотам
    "enable_admin_notify": true,    # Уведомления админам
    "require_phone": true,           # Обязательный телефон
    "ask_comment": true              # Запрос комментария
  }
}
```

### Сообщения (персонализация)

```json
{
  "messages": {
    "welcome": "Добро пожаловать в {business_name}!",
    "success": "✅ Заявка #{id} принята!",
    "booking_cancelled": "✅ Запись отменена"
  }
}
```

## 📊 Админ-панель

### Возможности админ-бота:

1. **📊 Статистика**
   - Заказы за день/неделю/месяц
   - Выручка
   - Топ услуг

2. **📋 Заказы**
   - Список на сегодня/завтра/неделю
   - Фильтр по статусу
   - Детали заказа

3. **👥 Клиенты**
   - База всех клиентов
   - Топ-10 клиентов
   - История клиента

4. **⚙️ Настройки**
   - Просмотр настроек
   - Информация об услугах

### Запуск админ-бота:

```bash
python admin_bot/main.py --config configs/client_lite.json
```

## 🗄️ База данных

Бот использует SQLite. База создаётся автоматически при первом запуске.

### Таблицы:

- `users` - пользователи бота
- `orders` - заказы/записи

### Расположение:

`db_{business_slug}.sqlite` (например, `db_barbershop_style.sqlite`)

## 🐛 Исправленные критические ошибки

### ✅ Ошибка #1: Дублирование сообщения о комментарии
**Исправлено в:** `handlers/booking.py` (строка ~356)

**Было:** Два одинаковых сообщения "Хотите добавить комментарий?"

**Стало:** Одно сообщение с inline-кнопками

### ✅ Ошибка #2: Кнопка "Назад" не работала
**Исправлено в:** `handlers/mybookings.py` (строка ~498)

**Было:** Использовался `show_my_bookings(callback.message, ...)`

**Стало:** Переписана логика с использованием `callback.message.answer()`

## 📝 Changelog

### Version 2.0.1 (26.12.2025)
- ✅ Исправлена ошибка дублирования сообщения о комментарии
- ✅ Исправлена кнопка "Назад" в редактировании записей
- ✅ Добавлен интерактивный установщик `setup.py`
- ✅ Добавлен админ-бот с статистикой
- ✅ Добавлена история клиента в уведомлениях

### Version 2.0.0 (25.12.2025)
- ✅ Полная переработка архитектуры на aiogram 3.x
- ✅ Система бронирования по слотам
- ✅ Редактирование записей
- ✅ Отправка номера через контакт
- ✅ Русские дни недели
- ✅ Формат времени HH:MM

## 🆘 Поддержка

### Частые вопросы:

**Q: Как получить свой Telegram ID?**
A: Напишите боту @userinfobot

**Q: Как изменить услуги?**
A: Отредактируйте `configs/client_lite.json`

**Q: Как сменить график работы?**
A: Измените `booking.work_start` и `work_end` в конфиге

**Q: Можно ли использовать без админ-бота?**
A: Да! Админ-бот опционален. Уведомления будут приходить в основной бот.

**Q: Как настроить слоты по 30 минут?**
A: Измените `booking.slot_duration` на 30 в конфиге

### Логирование:

Все события логируются в консоль:
- INFO - обычные события
- ERROR - ошибки

### Отладка:

Для детального логирования измените в `main.py`:

```python
logging.basicConfig(
    level=logging.DEBUG,  # Вместо INFO
    # ...
)
```

## 📄 Лицензия

MIT License - используйте свободно для коммерческих целей.

## 👨‍💻 Разработка

### Требования:
- Python 3.9+
- aiogram 3.15.0+
- SQLite3

### Разработка новых функций:

1. Создайте новый handler в `handlers/`
2. Зарегистрируйте router в `main.py`
3. Добавьте FSM состояния в `states/` (если нужно)

### Тестирование:

```bash
# Запуск с тестовым конфигом
python main.py --config configs/test_config.json
```

## 📮 Контакты

По вопросам доработки и поддержки обращайтесь к разработчику.

---

**Версия:** 2.0.1  
**Дата:** 26.12.2025  
**Статус:** ✅ Production Ready
