# Digital Admin — установка на VPS (Ubuntu 22.04) одной командой

## Требования VPS

Рекомендуется:
- 2 vCPU
- 2 GB RAM
- 20–30 GB SSD
- Ubuntu 22.04 LTS

Минимум (для малого бизнеса):
- 1 vCPU
- 1 GB RAM
- 20 GB SSD

## Что понадобится заранее

- Токен **клиентского** бота (BotFather)
- Токен **админского** бота (BotFather)
- Telegram ID владельца (можно узнать через @userinfobot)

## Установка (одна команда)

1) Загрузите проект на сервер (например, через git или scp) и перейдите в папку проекта.

2) Запустите установщик:

```bash
sudo bash deploy/linux/install.sh
```

Скрипт:
- установит Python и зависимости
- скопирует проект в `/opt/digital-admin`
- создаст venv и установит `requirements.txt`
- запустит интерактивный `python setup.py` (введёте токены/ID/название)
- создаст и запустит службы systemd

Примечание:
- при установке можно включить PIN для админ-панели (рекомендуется)

## Управление сервисами

Статус:
```bash
systemctl status digital-admin-client --no-pager
systemctl status digital-admin-admin  --no-pager
```

Перезапуск:
```bash
sudo systemctl restart digital-admin-client
sudo systemctl restart digital-admin-admin
```

Логи:
```bash
journalctl -u digital-admin-client -f
journalctl -u digital-admin-admin  -f
```

## Безопасность: PIN для админ-панели

PIN защищает админ-бота: даже если кто-то оказался в `admin_ids`, без PIN он не сможет управлять панелью.

Где хранится:
- `admin_pin_hash` (sha256) в `/opt/digital-admin/configs/client_lite.json`

Как включить при установке:
- во время `python setup.py` выберите установку PIN и введите 4+ цифры

Как сменить/отключить:
- в админ-боте: **Настройки** -> **🔐 PIN**

Если забыли PIN:
- самый простой способ:
```bash
sudo bash deploy/linux/reset_pin.sh
```

- вручную: откройте `/opt/digital-admin/configs/client_lite.json` и удалите поле `admin_pin_hash`, затем:
```bash
sudo systemctl restart digital-admin-admin
```

## Где лежат файлы

- Код и конфиги: `/opt/digital-admin`
- `.env` (токены): `/opt/digital-admin/.env`
- Конфиг бизнеса: `/opt/digital-admin/configs/client_lite.json`
- База данных: `/opt/digital-admin/db_<business_slug>.sqlite`

## Бэкапы (рекомендуется)

Делайте регулярный backup:
- `configs/client_lite.json`
- `db_<business_slug>.sqlite`

Пример ручного backup:
```bash
sudo mkdir -p /opt/digital-admin-backups
sudo cp /opt/digital-admin/configs/client_lite.json /opt/digital-admin-backups/client_lite.json.$(date +%F_%H%M%S)
sudo cp /opt/digital-admin/db_*.sqlite /opt/digital-admin-backups/ 2>/dev/null || true
```

Автобэкап (по умолчанию после установки):
- устанавливается systemd timer `digital-admin-backup.timer` (ежедневно)
- архивы сохраняются в `/opt/digital-admin-backups` в формате `digital-admin-backup.YYYY-MM-DD_HHMMSS.tar.gz`
- ротация по умолчанию: 14 дней (можно менять через переменную `KEEP_DAYS` внутри `deploy/linux/backup.sh`)

Проверить расписание:
```bash
systemctl list-timers digital-admin-backup.timer --no-pager
```

Запустить бэкап вручную:
```bash
sudo bash deploy/linux/backup.sh
```

Восстановление из бэкапа (откат):

```bash
sudo bash deploy/linux/restore.sh
```

Или явно указать архив:

```bash
sudo bash deploy/linux/restore.sh /opt/digital-admin-backups/digital-admin-backup.YYYY-MM-DD_HHMMSS.tar.gz
```

## Обновление (рекомендуется)

1) Загрузите новую версию проекта на сервер (в любую папку).

2) В папке проекта запустите:

```bash
sudo bash deploy/linux/update.sh
```

Скрипт:
- остановит сервисы
- сделает backup `configs/client_lite.json` и `db_*.sqlite`
- обновит код в `/opt/digital-admin` (не трогая `.env`, `configs/`, `db_*.sqlite`)
- обновит зависимости из `requirements.txt`
- запустит сервисы обратно

## Диагностика (health-check)

Если бот «не отвечает» или сервисы упали, в папке проекта запустите:

```bash
sudo bash deploy/linux/status.sh
```

Быстрая проверка после установки/обновления:
```bash
sudo bash deploy/linux/smoke_test.sh
```

Скрипт покажет:
- наличие `/opt/digital-admin`, `.env`, конфига и базы
- статус systemd сервисов
- последние строки логов (client/admin)

Дополнительно:
- версия схемы БД и таблицы/колонки:
```bash
sudo bash deploy/linux/db_info.sh
```

Примечание по обновлениям:
- при запуске сервисов бот автоматически применяет миграции SQLite (создаёт недостающие таблицы/колонки)

## Troubleshooting (решение типичных проблем)

### Бот не отвечает на сообщения

**Проверьте:**
1. Статус сервисов:
```bash
systemctl status digital-admin-client --no-pager
systemctl status digital-admin-admin --no-pager
```

2. Логи на ошибки:
```bash
journalctl -u digital-admin-client -n 100 --no-pager
journalctl -u digital-admin-admin -n 100 --no-pager
```

**Частые причины:**
- Неверный токен в `.env` → отредактируйте `/opt/digital-admin/.env` и перезапустите сервисы
- Сломанный конфиг JSON → проверьте `/opt/digital-admin/configs/client_lite.json` на валидность
- База данных заблокирована → проверьте права доступа к `db_*.sqlite`

### Service failed to start

**Проверьте детали:**
```bash
journalctl -xe
sudo bash deploy/linux/status.sh
```

**Частые причины:**
- Ошибка миграции БД → восстановите из бэкапа: `sudo bash deploy/linux/restore.sh`
- Отсутствует `.env` → запустите `sudo bash deploy/linux/install.sh` заново
- Конфликт портов (если используете webhook) → проверьте занятость портов

### Database is locked

**Решение:**
1. Остановите оба сервиса:
```bash
sudo systemctl stop digital-admin-client
sudo systemctl stop digital-admin-admin
```

2. Проверьте права на файлы БД:
```bash
ls -la /opt/digital-admin/db_*.sqlite*
sudo chown digitaladmin:digitaladmin /opt/digital-admin/db_*.sqlite*
```

3. Запустите сервисы:
```bash
sudo systemctl start digital-admin-client
sudo systemctl start digital-admin-admin
```

### Забыли PIN админ-панели

```bash
sudo bash deploy/linux/reset_pin.sh
```

### Бот работал, но после обновления сломался

**Откат к последнему бэкапу:**
```bash
sudo bash deploy/linux/restore.sh
```

Выберите архив перед обновлением и восстановите.

### Permission denied при запуске скриптов

```bash
chmod +x /opt/digital-admin/deploy/linux/*.sh
```

### Проверка версии схемы БД

```bash
sudo bash deploy/linux/db_info.sh
```

Если версия схемы не соответствует ожидаемой (должна быть 2), возможно миграции не применились. Проверьте логи при старте сервиса.

### Нет свободного места на диске

**Проверка:**
```bash
df -h
```

**Очистка старых бэкапов:**
```bash
ls -lh /opt/digital-admin-backups/
# Удалите старые вручную или измените KEEP_DAYS в backup.sh
```

### Дополнительная помощь

Если проблема не решена:
1. Запустите полную диагностику: `sudo bash deploy/linux/smoke_test.sh`
2. Сохраните вывод и логи для анализа
3. Проверьте, что все зависимости установлены: `python3 --version`, `pip3 --version`

## Мониторинг

### Health Check

Автоматическая проверка состояния системы:
```bash
sudo bash deploy/linux/health_check.sh
```

Скрипт проверяет:
- Статус сервисов (client/admin)
- Доступность БД
- Свободное место на диске
- Количество ошибок в логах

Exit code:
- `0` — всё в порядке
- `1` — есть проблемы

Можно добавить в cron для автоматического мониторинга:
```bash
# Проверка каждые 5 минут
*/5 * * * * /opt/digital-admin/deploy/linux/health_check.sh || echo "Health check failed" | mail -s "Digital Admin Alert" admin@example.com
```

### Логи

Логи сохраняются в двух местах:
1. **journalctl** (systemd):
```bash
journalctl -u digital-admin-client -f
journalctl -u digital-admin-admin -f
```

2. **Файлы** (с ротацией):
- `/opt/digital-admin/logs/client_bot.log` (до 50MB, 5 файлов)
- `/opt/digital-admin/logs/admin_bot.log` (до 50MB, 5 файлов)
