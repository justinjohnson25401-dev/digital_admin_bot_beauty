# 🚀 БЫСТРЫЙ СТАРТ - 5 минут до запуска

## Шаг 1: Установка зависимостей (30 сек)

```bash
pip install -r requirements.txt
```

**Что установится:**
- `aiogram==3.15.0` - библиотека для Telegram ботов
- `python-dotenv==1.0.0` - работа с переменными окружения
- `apscheduler==3.10.4` - планировщик задач

---

## Шаг 2: Получение токенов (2 мин)

### 2.1 Создайте ботов в Telegram

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте `/newbot`
3. Следуйте инструкциям:
   - **Для клиентского бота:** `Ваш Бизнес Client Bot`
   - **Для админ-бота:** `Ваш Бизнес Admin Bot`
4. Скопируйте токены (выглядят как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2.2 Узнайте свой Telegram ID

1. Напишите боту **@userinfobot**
2. Скопируйте ваш ID (например, `123456789`)

---

## Шаг 3: Настройка файлов (1 мин)

### 3.1 Создайте .env файл

```bash
cp .env.example .env
nano .env  # или откройте в любом редакторе
```

**Вставьте ваши токены:**
```env
CLIENT_BOT_TOKEN=ваш_токен_клиентского_бота
ADMIN_BOT_TOKEN=ваш_токен_админ_бота
ADMIN_PIN=1234
```

### 3.2 Настройте конфигурацию

Файл `configs/client_lite.json` уже существует, но проверьте:

```bash
nano configs/client_lite.json  # или любой редактор
```

**Измените:**
- `admin_ids` - ваш Telegram ID
- `business_name` - название вашего бизнеса
- `business_slug` - уникальный идентификатор (латиница)
- `services` - список услуг и цен

**Пример:**
```json
{
  "admin_ids": [123456789],
  "business_name": "Барбершоп Стиль",
  "business_slug": "barbershop_style",
  "services": [
    {"id": "service1", "name": "Стрижка", "price": 1500},
    {"id": "service2", "name": "Борода", "price": 800}
  ]
}
```

---

## Шаг 4: Запуск (30 сек)

### Вариант 1: Только клиентский бот

```bash
python main.py --config configs/client_lite.json
```

### Вариант 2: Оба бота (рекомендуется)

**Терминал 1 - Клиентский бот:**
```bash
python main.py --config configs/client_lite.json
```

**Терминал 2 - Админ-бот:**
```bash
python admin_bot/main.py --config configs/client_lite.json
```

---

## Шаг 5: Проверка работы (1 мин)

1. **Откройте клиентского бота** в Telegram
2. Отправьте `/start`
3. Вы должны увидеть главное меню
4. **Откройте админ-бота** (если запустили)
5. Отправьте `/start` и введите PIN (по умолчанию `1234`)

✅ **Готово!** Бот работает!

---

## 🔧 Дополнительные настройки

### Изменить услуги и цены

Отредактируйте `configs/client_lite.json`:

```json
{
  "services": [
    {"id": "service1", "name": "Моя Услуга", "price": 2000, "duration": 60}
  ]
}
```

Бот автоматически подхватит изменения через 3 секунды!

### Изменить график работы

```json
{
  "booking": {
    "work_start": 9,      # Начало (9:00)
    "work_end": 21,       # Конец (21:00)
    "slot_duration": 30   # Слоты по 30 минут
  }
}
```

### Изменить FAQ

```json
{
  "faq": [
    {"btn": "💰 Цены", "answer": "Наши цены:\n• Услуга 1 — 1000₽"},
    {"btn": "📍 Адрес", "answer": "г. Москва, ул. Примерная, д. 1"}
  ]
}
```

---

## ❓ Проблемы?

### Ошибка: "BOT_TOKEN не найден"
- Проверьте что создали `.env` файл
- Убедитесь что токены правильные (без пробелов)

### Ошибка: "Database initialization error"
- Проверьте права на запись в текущую директорию
- База создается автоматически как `db_ваш_slug.sqlite`

### Бот не отвечает
- Проверьте что токен правильный
- Убедитесь что бот не заблокирован в Telegram
- Проверьте интернет-соединение

### Админ-бот просит PIN
- PIN по умолчанию: `1234`
- Измените в `.env`: `ADMIN_PIN=ваш_пин`

---

## 📚 Дополнительная информация

- **README.md** - полная документация
- **CODE_REVIEW.md** - отчет о проверке кода (5/5 ⭐)
- **TESTING_GUIDE.md** - руководство по тестированию
- **deploy/linux/** - скрипты для развертывания на сервере

---

## 🎉 Готово!

Теперь у вас работающий Telegram-бот для приема заказов!

**Следующие шаги:**
1. Поделитесь ботом с клиентами
2. Настройте услуги под ваш бизнес
3. Следите за заказами через админ-бота

**Удачи! 🚀**
