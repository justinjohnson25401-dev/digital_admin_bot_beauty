## [2026-01-04] - Улучшение стабильности (Gemini)

### 🔧 Исправлено (Fixed)
- **Отсутствие FSM Storage:**
  - **Проблема:** Состояния пользователей (FSM) сбрасывались при перезапуске бота.
  - **Решение:** В `main.py` внедрено персистентное хранилище `SQLiteStorage`. Теперь состояния сохраняются в файле `fsm_data_{business_slug}.db`, что предотвращает потерю данных при перезагрузках.
  - **Статус:** ✅ **ИСПРАВЛЕНО**

- **Race Condition при бронировании:**
  - **Проблема:** Два пользователя могли одновременно забронировать один и тот же временной слот.
  - **Решение:** Метод `add_order` в `utils/db/booking_queries.py` был обернут в атомарную транзакцию (`with self.connection:`).
  - **Статус:** ✅ **ИСПРАВЛЕНО**

### 📝 Документация (Docs)
- Обновлен `.docs/BUGS_TRACKER.md`: баги `Race Condition` и `FSM Storage` перенесены в раздел исправленных.

---

## [2026-01-03] - Рефакторинг архитектуры (Firebase Studio)

### ➕ Добавлено (Added)
- `admin_handlers/staff/` - модульная структура управления персоналом
- `handlers/booking/` - модульная структура процесса бронирования

### 🔄 Изменено (Changed)
- Архитектура проекта: переход на модульную структуру

### ❌ Удалено (Removed)
- `admin_handlers/staff_editor.py` (1478 строк монолита)
- `handlers/booking.py` (1020 строк монолита)

### 🔧 Исправлено (Fixed)
- Убраны заглушки `# TITLE ...` из кода
- Добавлены недостающие параметры в функции (`config_manager`, `db_manager`)
- Исправлены импорты в `admin_bot/main.py`
- FSM states корректно работают после рефакторинга
