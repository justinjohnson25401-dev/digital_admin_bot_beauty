# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ СИСТЕМЫ

Полное руководство по тестированию Telegram-бота для записи клиентов.

---

## 📋 СОДЕРЖАНИЕ

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Локальное тестирование](#локальное-тестирование)
3. [Тестирование на VPS](#тестирование-на-vps)
4. [Функциональное тестирование](#функциональное-тестирование)
5. [Нагрузочное тестирование](#нагрузочное-тестирование)
6. [Тестирование безопасности](#тестирование-безопасности)
7. [Чек-лист перед production](#чек-лист-перед-production)

---

## 1. ПОДГОТОВКА К ТЕСТИРОВАНИЮ

### 1.1. Что понадобится

- [ ] 2 тестовых Telegram бота (от @BotFather)
- [ ] Минимум 2 тестовых Telegram аккаунта
- [ ] VPS с Ubuntu 22.04 (для staging)
- [ ] Текстовый редактор для логов
- [ ] Telegram Desktop (для быстрого переключения аккаунтов)

### 1.2. Создание тестовых ботов

```bash
# В Telegram найдите @BotFather
/newbot
# Название: Test Business Bot
# Username: test_business_12345_bot

/newbot
# Название: Test Admin Bot
# Username: test_admin_12345_bot

# Сохраните оба токена
```

### 1.3. Получение Telegram ID

```bash
# Найдите @userinfobot
# Отправьте /start
# Сохраните ваш ID (например: 123456789)
```

---

## 2. ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ

### 2.1. Установка и настройка

```bash
# Клонируйте проект
cd bot_business_v2_final

# Создайте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите setup
python setup.py
```

**Введите тестовые данные:**
- BOT_TOKEN: `токен_от_BotFather`
- ADMIN_BOT_TOKEN: `токен_админ_бота`
- Telegram ID: `ваш_ID`
- Название: `Test Barbershop`
- Slug: `test_barber`
- PIN: `1234` (для тестирования)

### 2.2. Запуск ботов локально

**Терминал 1 (Client Bot):**
```bash
python main.py --config configs/client_lite.json
```

**Терминал 2 (Admin Bot):**
```bash
python admin_bot/main.py --config configs/client_lite.json
```

### 2.3. Проверка логов

```bash
# Логи должны показывать:
✅ Конфигурация загружена: Test Barbershop
✅ База данных инициализирована: db_test_barber.sqlite
✅ Database schema version: 2
🚀 Бот 'Test Barbershop' запущен!
```

---

## 3. ТЕСТИРОВАНИЕ НА VPS

### 3.1. Развертывание на staging

```bash
# На VPS
sudo bash deploy/linux/install.sh

# Проверка установки
sudo bash deploy/linux/status.sh
sudo bash deploy/linux/smoke_test.sh
```

### 3.2. Проверка сервисов

```bash
# Статус
systemctl status digital-admin-client --no-pager
systemctl status digital-admin-admin --no-pager

# Логи в реальном времени
journalctl -u digital-admin-client -f
journalctl -u digital-admin-admin -f

# Файловые логи
tail -f /opt/digital-admin/logs/client_bot.log
tail -f /opt/digital-admin/logs/admin_bot.log
```

### 3.3. Health Check

```bash
# Ручная проверка
sudo bash deploy/linux/health_check.sh

# Должно показать:
✅ digital-admin-client: running
✅ digital-admin-admin: running
✅ Database: accessible
✅ Disk space: 25%
✅ Client bot: 0 errors
✅ Admin bot: 0 errors
✅ All checks passed
```

---

## 4. ФУНКЦИОНАЛЬНОЕ ТЕСТИРОВАНИЕ

### 4.1. Тестирование клиентского бота

#### Тест 1: Первый запуск
```
Действие: /start
Ожидаемый результат:
- Приветственное сообщение
- Главное меню с кнопками:
  📅 Записаться / Заказать
  📋 Мои записи
  ❓ FAQ
```

#### Тест 2: Создание записи (полный flow)
```
1. Нажать "📅 Записаться / Заказать"
2. Выбрать услугу (например, "Стрижка")
3. Выбрать дату (например, "Завтра")
4. Выбрать время (например, "14:00")
5. Ввести имя: "Иван"
6. Отправить телефон через контакт ИЛИ ввести вручную: +79991234567
7. Добавить комментарий: "Хочу короткую стрижку" ИЛИ пропустить
8. Подтвердить запись

Ожидаемый результат:
- Подтверждение записи с деталями
- Запись появляется в "📋 Мои записи"
- Админ получает уведомление с историей клиента
```

#### Тест 3: Редактирование записи
```
1. Открыть "📋 Мои записи"
2. Нажать "✏️ Изменить #ID"
3. Изменить дату/время/услугу
4. Подтвердить изменения

Ожидаемый результат:
- Запись обновлена
- Админ получил уведомление об изменении
```

#### Тест 4: Отмена записи (с подтверждением)
```
1. Открыть "📋 Мои записи"
2. Нажать "🗑 Отменить #ID"
3. Появляется подтверждение: "Вы уверены?"
4. Нажать "✅ Да, отменить"

Ожидаемый результат:
- Запись отменена
- Запись исчезла из "Мои записи"
- Админ получил уведомление
```

#### Тест 5: Race condition (двойное бронирование)
```
1. Пользователь A выбирает слот 14:00 на завтра
2. Пользователь B ОДНОВРЕМЕННО выбирает тот же слот
3. Один из них подтверждает первым

Ожидаемый результат:
- Первый получает подтверждение
- Второй получает ошибку "Слот уже занят"
```

#### Тест 6: Валидация телефона
```
Попробуйте ввести:
- 89991234567 → преобразуется в +79991234567 ✅
- +79991234567 → принимается ✅
- 79991234567 → преобразуется в +79991234567 ✅
- 123 → ошибка "Некорректный формат" ❌
- abcd → ошибка "Некорректный формат" ❌
```

### 4.2. Тестирование админ-бота

#### Тест 7: PIN-авторизация
```
1. /start в админ-боте
2. Ввести неверный PIN: "0000"
   → Блокировка на 30 сек
3. Подождать 30 сек
4. Ввести правильный PIN: "1234"
   → Доступ к админ-панели

Ожидаемый результат:
- Защита от перебора работает
- После правильного PIN — полный доступ
```

#### Тест 8: Просмотр статистики
```
1. Нажать "📊 Статистика"

Ожидаемый результат:
- Показывает заказы за сегодня/неделю/месяц
- Выручка корректна
- Топ услуг отображается
```

#### Тест 9: Просмотр заказов
```
1. Нажать "📋 Заказы"
2. Выбрать период (сегодня/завтра/неделя)
3. Открыть детали заказа

Ожидаемый результат:
- Список заказов с пагинацией
- История клиента в деталях
- Контакты клиента
```

#### Тест 10: Редактирование услуг
```
1. Нажать "📝 Услуги"
2. Добавить новую услугу: "Борода" - 800₽
3. Изменить цену существующей
4. Удалить услугу

Ожидаемый результат:
- Изменения сохраняются
- config_version увеличивается
- Client bot подхватывает изменения через 3 сек
```

#### Тест 11: Изменение настроек
```
1. Нажать "⚙️ Настройки"
2. Изменить название бизнеса
3. Изменить график работы: 9:00-21:00
4. Изменить длительность слота: 30 мин
5. Изменить таймзону

Ожидаемый результат:
- Все изменения применяются
- Client bot перезагружает конфиг
```

#### Тест 12: Управление PIN
```
1. "⚙️ Настройки" → "🔐 PIN"
2. Изменить PIN на "5678"
3. Выйти и войти снова
4. Ввести новый PIN

Ожидаемый результат:
- Старый PIN не работает
- Новый PIN работает
```

#### Тест 13: Экспорт CSV
```
1. Нажать "📊 Статистика"
2. Нажать "📥 Экспорт CSV"

Ожидаемый результат:
- Получен CSV файл
- Содержит все заказы за 30 дней
- Кодировка UTF-8-BOM (открывается в Excel)
```

---

## 5. НАГРУЗОЧНОЕ ТЕСТИРОВАНИЕ

### 5.1. Тест параллельных записей

```python
# test_concurrent_bookings.py
import asyncio
from aiogram import Bot

async def create_booking(bot_token, user_id):
    bot = Bot(token=bot_token)
    # Симуляция создания записи
    # ...

# Запустить 10 параллельных записей на один слот
asyncio.run(test_race_condition())
```

**Ожидаемый результат:**
- Только 1 запись успешна
- Остальные получают ошибку "Слот занят"

### 5.2. Тест производительности БД

```bash
# Создать 1000 тестовых записей
python scripts/generate_test_data.py --orders 1000

# Проверить скорость запросов
time sqlite3 db_test_barber.sqlite "SELECT * FROM orders WHERE status='active'"

# Должно быть < 100ms
```

### 5.3. Тест ротации логов

```bash
# Сгенерировать 100MB логов
for i in {1..10000}; do
  echo "Test log entry $i with some data" >> logs/client_bot.log
done

# Проверить ротацию
ls -lh logs/

# Должно быть:
# client_bot.log (текущий)
# client_bot.log.1
# client_bot.log.2
# ...
# client_bot.log.5 (не больше 5)
```

---

## 6. ТЕСТИРОВАНИЕ БЕЗОПАСНОСТИ

### 6.1. SQL Injection

```
Попробуйте в комментарии к записи:
- '; DROP TABLE orders; --
- ' OR '1'='1
- <script>alert('xss')</script>

Ожидаемый результат:
- Все сохраняется как обычный текст
- Никаких SQL ошибок
- Никакого выполнения кода
```

### 6.2. Rate Limiting PIN

```
1. Попробуйте ввести неверный PIN 10 раз подряд
2. Подождите 1 час
3. Попробуйте снова

Ожидаемый результат:
- После 3 попыток: блокировка на 30 сек
- После 4 попыток: блокировка на 60 сек
- После 10 попыток в час: "Превышен лимит"
```

### 6.3. Доступ к чужим записям

```
1. Пользователь A создает запись (ID=123)
2. Пользователь B пытается отменить запись A через callback_data

Ожидаемый результат:
- "Это не ваш заказ"
- Запись не отменяется
```

### 6.4. Проверка .gitignore

```bash
# Убедитесь, что секреты не в git
git status

# НЕ должно быть:
# .env
# db_*.sqlite
# configs/client_lite.json (если не template)
```

---

## 7. ЧЕК-ЛИСТ ПЕРЕД PRODUCTION

### 7.1. Конфигурация

- [ ] `.env` содержит реальные токены
- [ ] `admin_ids` содержит реальные ID
- [ ] `business_name` корректен
- [ ] `business_slug` уникален
- [ ] PIN установлен и сложный (не 1234)
- [ ] График работы корректен
- [ ] Услуги и цены актуальны
- [ ] FAQ заполнен

### 7.2. Инфраструктура

- [ ] VPS имеет минимум 1GB RAM
- [ ] Свободно минимум 10GB диска
- [ ] SSH доступ настроен
- [ ] Firewall настроен (только 22, 80, 443)
- [ ] Часовой пояс сервера корректен

### 7.3. Сервисы

- [ ] `systemctl status digital-admin-client` → active
- [ ] `systemctl status digital-admin-admin` → active
- [ ] `systemctl list-timers` → backup timer active
- [ ] `sudo bash deploy/linux/health_check.sh` → все ✅

### 7.4. Безопасность

- [ ] PIN не 1234 или другой простой
- [ ] `.env` не в git
- [ ] Backup включен и работает
- [ ] Логи ротируются
- [ ] Health check настроен

### 7.5. Тестирование

- [ ] Создание записи работает
- [ ] Редактирование записи работает
- [ ] Отмена записи работает (с подтверждением)
- [ ] Уведомления админам приходят
- [ ] История клиента показывается
- [ ] Админ-панель доступна с PIN
- [ ] Экспорт CSV работает
- [ ] Race condition обрабатывается

### 7.6. Мониторинг

- [ ] Логи пишутся в `/opt/digital-admin/logs/`
- [ ] `journalctl` показывает логи
- [ ] `health_check.sh` возвращает exit 0
- [ ] Backup создается ежедневно
- [ ] Можно восстановить из backup

---

## 8. ТИПИЧНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: Бот не отвечает

**Диагностика:**
```bash
sudo bash deploy/linux/status.sh
journalctl -u digital-admin-client -n 100
```

**Решения:**
- Проверить токен в `.env`
- Проверить интернет на VPS
- Перезапустить: `sudo systemctl restart digital-admin-client`

### Проблема: Database is locked

**Решение:**
```bash
sudo systemctl stop digital-admin-client
sudo systemctl stop digital-admin-admin
sudo chown digitaladmin:digitaladmin /opt/digital-admin/db_*.sqlite*
sudo systemctl start digital-admin-client
sudo systemctl start digital-admin-admin
```

### Проблема: Миграции не применились

**Диагностика:**
```bash
sudo bash deploy/linux/db_info.sh
# Должно показать: schema_version: 2
```

**Решение:**
```bash
sudo bash deploy/linux/restore.sh
# Выбрать последний backup
```

### Проблема: Логи не пишутся

**Проверка:**
```bash
ls -la /opt/digital-admin/logs/
# Должны быть client_bot.log и admin_bot.log
```

**Решение:**
```bash
sudo mkdir -p /opt/digital-admin/logs
sudo chown digitaladmin:digitaladmin /opt/digital-admin/logs
sudo systemctl restart digital-admin-client
sudo systemctl restart digital-admin-admin
```

---

## 9. АВТОМАТИЗАЦИЯ ТЕСТИРОВАНИЯ

### 9.1. Скрипт полного теста

```bash
#!/bin/bash
# test_all.sh

echo "=== Running Full Test Suite ==="

# 1. Health check
echo "1. Health check..."
sudo bash deploy/linux/health_check.sh || exit 1

# 2. DB check
echo "2. Database check..."
sudo bash deploy/linux/db_info.sh | grep "schema_version: 2" || exit 1

# 3. Services check
echo "3. Services check..."
systemctl is-active --quiet digital-admin-client || exit 1
systemctl is-active --quiet digital-admin-admin || exit 1

# 4. Logs check
echo "4. Logs check..."
test -f /opt/digital-admin/logs/client_bot.log || exit 1
test -f /opt/digital-admin/logs/admin_bot.log || exit 1

# 5. Backup check
echo "5. Backup check..."
test -d /opt/digital-admin-backups || exit 1
ls /opt/digital-admin-backups/*.tar.gz >/dev/null 2>&1 || exit 1

echo "✅ All tests passed!"
```

### 9.2. Cron для регулярного тестирования

```bash
# Добавить в crontab
crontab -e

# Каждый час
0 * * * * /opt/digital-admin/deploy/linux/health_check.sh || echo "Health check failed" | mail -s "Alert" admin@example.com

# Каждый день в 3:00
0 3 * * * /opt/digital-admin/test_all.sh >> /var/log/bot-tests.log 2>&1
```

---

## 10. ДОКУМЕНТАЦИЯ РЕЗУЛЬТАТОВ

### Шаблон отчета о тестировании

```markdown
# Отчет о тестировании

**Дата:** 27.12.2024
**Тестировщик:** Иван Иванов
**Версия:** v2.0
**Окружение:** Production VPS

## Результаты

| Тест | Статус | Комментарий |
|------|--------|-------------|
| Создание записи | ✅ | Работает корректно |
| Редактирование | ✅ | Работает |
| Отмена с подтверждением | ✅ | Работает |
| Race condition | ✅ | Защита работает |
| PIN авторизация | ✅ | Rate limiting работает |
| Уведомления админам | ✅ | Приходят с историей |
| Health check | ✅ | Все проверки пройдены |
| Backup/Restore | ✅ | Работает |

## Найденные проблемы

Нет

## Рекомендации

- Настроить мониторинг через cron
- Добавить напоминания клиентам
```

---

## ЗАКЛЮЧЕНИЕ

Эта инструкция покрывает все аспекты тестирования системы. Следуйте чек-листам перед каждым деплоем и регулярно проверяйте health check.

**Минимальный набор тестов перед production:**
1. ✅ Создание/редактирование/отмена записи
2. ✅ PIN авторизация админа
3. ✅ Health check проходит
4. ✅ Backup работает
5. ✅ Логи пишутся

**Удачи в production! 🚀**
