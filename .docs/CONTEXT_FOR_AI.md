# 📘 КОНТЕКСТ ПРОЕКТА ДЛЯ AI

> Прочитай этот файл ПОЛНОСТЬЮ перед началом работы (время чтения: ~10 минут)

---

## 🎯 ЧТО ЭТО ЗА ПРОЕКТ

**Название:** Telegram-бот для салонов красоты  
**Цель:** Автоматизация записи клиентов (замена звонков и ручных записей)  
**Формат:** SaaS-продукт (продажа ботов салонам красоты)  
**Целевая аудитория:** Малый бизнес (салоны, студии, соло-мастера)

**Репозиторий:** https://github.com/justinjohnson25401-dev/digital_admin_bot_beauty

**Версия:** 2.7.0 (от 02.01.2026)  
**Статус:** Финальная стадия, исправление багов перед запуском продаж

---

## 👤 ВЛАДЕЛЕЦ ПРОЕКТА

**Имя:** Justin  
**GitHub:** @justinjohnson25401  
**Опыт программирования:** Низкий (НЕ разработчик!)  
**Роль:** Предприниматель, продавец SaaS-продукта

### Цели владельца:
1. Продать 20-100 ботов салонам красоты
2. Цена: 5000₽ разово + 500₽/мес за VPS
3. Минимум обращений после продажи (клиенты сами настраивают)
4. Быстрее закончить разработку (работает 10-14 недель, устал)

### Важно для AI:
- ⚠️ Владелец НЕ программист → **объясняй простыми словами, БЕЗ кода**
- ⚠️ Будь конкретен: **"что делать"**, а не **"как реализовать"**
- ⚠️ Спрашивай уточнения СРАЗУ, не додумывай сам
- ⚠️ Фокус на бизнес-результат (продажи), а не "красивый код"

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА

### Технологии:
- **Язык:** Python 3.10+
- **Фреймворк бота:** aiogram 3.x (асинхронный)
- **База данных:** SQLite (локально, один файл)
- **FSM:** aiogram FSM (конечный автомат состояний)
- **Тесты:** pytest (73 теста, покрытие ~70%)
- **Логирование:** logging (с маскировкой персональных данных)

### Два бота (два отдельных процесса):

#### 1️⃣ Клиентский бот (`main.py`)
**Для кого:** Клиенты салона (обычные пользователи)

**Функции:**
- Запись на услуги (FSM процесс, 10 шагов)
- Просмотр своих записей
- Редактирование записей
- Отмена записей
- FAQ (часто задаваемые вопросы)
- Информация о салоне (адрес, телефон, соцсети)

#### 2️⃣ Админ-панель (`admin_bot/main.py`)
**Для кого:** Владелец салона (администратор)

**Функции:**
- Просмотр всех заказов (сегодня, неделя, месяц)
- Подтверждение/отмена заказов
- База клиентов (имена, телефоны, статистика)
- Статистика (количество записей, выручка)
- Настройки (частично)
- Получение уведомлений о новых записях

### Общие ресурсы:
- **База данных:** `db_mini_studio.sqlite` (одна для обоих ботов)
- **Конфигурация:** `configs/client_lite.json` (настройки салона)
- **Логи:** Разные файлы для каждого бота

**Важно:** Оба бота работают ОДНОВРЕМЕННО (два окна CMD на Windows)

---

## 📂 СТРУКТУРА ПРОЕКТА

```
digital_admin_bot_beauty/
├── main.py                      # Точка входа клиентского бота
├── setup.py                     # Установщик (первоначальная настройка)
├── requirements.txt             # Зависимости Python
├── .env                         # Токены ботов (НЕ в git!)
│
├── admin_bot/
│   └── main.py                 # Точка входа админ-панели
│
├── handlers/                    # Обработчики клиентского бота
│   ├── start.py                # Главное меню, /start, Акции, FAQ
│   ├── booking.py              # FSM процесс записи (10 шагов)
│   ├── mybookings.py           # Просмотр/редактирование записей
│   └── info.py                 # Информация о салоне
│
├── admin_handlers/              # Обработчики админ-панели
│   ├── orders.py               # Управление заказами
│   ├── clients.py              # База клиентов
│   └── settings.py             # Настройки салона
│
├── utils/                       # Утилиты (общие для обоих ботов)
│   ├── db_manager.py           # Работа с БД (CRUD операции)
│   ├── validators.py           # Валидация данных (телефон, дата)
│   ├── calendar.py             # Календарь выбора даты (inline клавиатура)
│   ├── notify.py               # Уведомления админу (новая запись, отмена)
│   └── privacy.py              # Маскировка PII в логах (имена, телефоны)
│
├── configs/
│   └── client_lite.json        # Конфиг салона (услуги, мастера, график)
│
├── tests/                       # Тесты (73 теста)
│   ├── test_db_manager.py
│   ├── test_booking.py
│   ├── test_validators.py
│   ├── test_calendar.py
│   └── ...
│
├── .docs/                       # Документация для AI (ты здесь!)
│   ├── CONTEXT_FOR_AI.md       # Этот файл
│   ├── PROJECT_STATE.md        # Текущее состояние
│   ├── BUGS_TRACKER.md         # Трекер багов
│   ├── CHANGELOG_AI.md         # История изменений
│   ├── versions/               # Бэкапы состояния
│   └── archive/                # Архив старых записей
│
├── docs/                        # Документация для людей
│   ├── QUICKSTART.md
│   ├── CHANGELOG.md
│   ├── BUGFIXES.md
│   └── ...
│
└── README.md                    # Описание проекта для GitHub
```

---

## 🔄 ПРОЦЕСС ЗАПИСИ (FSM) — ГЛАВНАЯ ФУНКЦИЯ БОТА

**10 шагов** (самая важная часть проекта):

### Шаг 1: Начало
- Клиент нажимает кнопку **"📅 Записаться"** в главном меню
- Бот переводит FSM в состояние `selecting_service`

### Шаг 2: Выбор услуги
- Показывается список услуг из `configs/client_lite.json`
- Пример: "Маникюр гель-лак (1500₽, 90 мин)"
- Клиент выбирает → FSM → `selecting_master`

### Шаг 3: Выбор мастера
- Показывается список мастеров (кто делает выбранную услугу)
- Пример: "Мария (рейтинг 4.9)"
- Если мастер один → пропускается автоматически
- FSM → `selecting_date`

### Шаг 4: Выбор даты
- Показывается inline-календарь (на N дней вперёд из конфига)
- Клиент выбирает дату → FSM → `selecting_time`

### Шаг 5: Выбор времени
- Показывается список свободных слотов (например: 10:00, 11:30, 14:00)
- Слоты рассчитываются на основе:
  - График работы мастера
  - Длительность услуги
  - Уже занятые слоты (из БД)
- Клиент выбирает → FSM → `entering_name`

### Шаг 6: Ввод имени
- Если клиент записывается ПЕРВЫЙ раз → просит ввести имя
- Если уже записывался → берёт из БД (таблица `users`)
- Валидация: 2-50 символов, только буквы
- FSM → `entering_phone`

### Шаг 7: Ввод телефона
- Просит ввести номер телефона
- Валидация: российский формат (+7 или 8, 10 цифр)
- Пример: +7 905 123 45 67 или 89051234567
- FSM → `entering_comment`

### Шаг 8: Комментарий (опционально)
- Просит ввести комментарий или нажать "Пропустить"
- Клиент может написать пожелания
- FSM → `confirming`

### Шаг 9: Подтверждение
- Показывается итоговое окно:
  ```
  Подтверждение записи

  Услуга: Маникюр гель-лак
  Мастер: Мария
  Дата: 15 января, 14:00
  Цена: 1500₽

  [✅ Подтвердить] [❌ Отменить]
  ```
- Клиент нажимает "Подтвердить" → FSM → завершение

### Шаг 10: Создание заказа
- Создаётся запись в таблице `orders` (БД)
- Отправляется уведомление админу (через админ-бот)
- Клиент получает подтверждение:
  ```
  ✅ Запись подтверждена!

  Услуга: Маникюр гель-лак
  Мастер: Мария
  Дата: 15 января, 14:00
  Цена: 1500₽

  Мы ждём вас! 🌟
  ```
- FSM → очистка состояния

### Важные детали:
- **Откат назад:** На каждом шаге есть кнопка "◀️ Назад"
- **Отмена:** Можно отменить запись в любой момент
- **Логирование:** Каждый шаг логируется (для отладки)
- **Защита от дублей:** Проверка занятости слота перед созданием заказа

---

## 💾 БАЗА ДАННЫХ (SQLite)

**Файл:** `db_mini_studio.sqlite`

### Таблица `users` — клиенты салона

| Поле | Тип | Описание |
|------|-----|----------|
| user_id | INTEGER PRIMARY KEY | Telegram ID клиента |
| name | TEXT | Имя клиента (Мария) |
| phone | TEXT | Телефон (+79051234567) |
| username | TEXT | Telegram username (@maria) |
| created_at | TEXT | Дата регистрации (ISO 8601) |

### Таблица `orders` — записи клиентов

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | Автоинкремент |
| user_id | INTEGER | FK → users.user_id |
| service_id | TEXT | ID услуги (manicure_gel) |
| service_name | TEXT | Название услуги (Маникюр гель-лак) |
| price | INTEGER | Цена услуги (1500) |
| master_id | TEXT | ID мастера (master_anna) |
| booking_date | TEXT | Дата записи (2026-01-15) |
| slot_time | TEXT | Время слота (14:00) |
| status | TEXT | pending/confirmed/completed/cancelled |
| discount_percent | INTEGER | Скидка в % (30) |
| discount_amount | INTEGER | Скидка в₽ (450) |
| final_price | INTEGER | Итоговая цена (1050) |
| comment | TEXT | Комментарий клиента |
| created_at | TEXT | Дата создания записи |
| updated_at | TEXT | Дата последнего изменения |

### Таблица `masters` (опционально, если используется)

| Поле | Тип | Описание |
|------|-----|----------|
| master_id | TEXT PRIMARY KEY | ID мастера (master_anna) |
| name | TEXT | Имя мастера (Анна) |
| services | TEXT | JSON массив услуг |
| schedule | TEXT | JSON график работы |
| rating | REAL | Рейтинг (4.9) |

### Важные операции:

**Защита от race condition:**
- Используются транзакции (`BEGIN TRANSACTION`)
- Блокировка записей (`FOR UPDATE`)
- Атомарные операции через `db_manager.py`

**Пример проблемы:**
- 2 клиента одновременно выбрали слот 14:00
- Оба проверили (свободен) → оба создали заказ
- **Решение:** Транзакция проверяет и создаёт в одной атомарной операции

---

## ⚙️ КОНФИГУРАЦИЯ (`configs/client_lite.json`)

**Этот файл редактируют клиенты (владельцы салонов) БЕЗ программирования!**

### Структура:

```json
{
  "business_name": "Студия Мощь",
  "business_type": "mini_studio",

  "services": [
    {
      "id": "manicure_gel",
      "name": "Маникюр гель-лак",
      "price": 1500,
      "duration": 90,
      "description": "Маникюр с покрытием гель-лаком"
    },
    ...
  ],

  "staff": [
    {
      "id": "master_anna",
      "name": "Анна",
      "services": ["manicure_gel", "pedicure"],
      "schedule": {
        "monday": {"start": "09:00", "end": "18:00"},
        "tuesday": {"start": "09:00", "end": "18:00"},
        ...
      }
    },
    ...
  ],

  "booking_settings": {
    "days_ahead": 14,
    "slot_duration": 30,
    "min_booking_time": 60
  },

  "faq": [
    {
      "question": "Как записаться?",
      "answer": "Нажмите кнопку 'Записаться' в главном меню"
    },
    ...
  ],

  "contact_info": {
    "address": "ул. Ленина, 123",
    "phone": "+7 905 123 45 67",
    "instagram": "@studio_mosh"
  }
}
```

### Важно:
- Клиенты должны понимать структуру JSON
- Валидация конфига при загрузке (через `validators.py`)
- Ошибки конфига → бот не запускается (с понятным сообщением)

---

## 🚀 ЗАПУСК ПРОЕКТА

### Первоначальная настройка (один раз):

```bash
python setup.py
```

**Что происходит:**
1. Спрашивает формат бизнеса (соло-мастер / мини-студия / салон)
2. Просит токены двух ботов (от @BotFather)
3. Просит Telegram ID владельца (от @userinfobot)
4. Название салона
5. Создаёт `.env`, `configs/client_lite.json`, `db_mini_studio.sqlite`

### Запуск клиентского бота:

```bash
python main.py --config configs/client_lite.json
```

**Логи в консоли:**
```
2026-01-02 21:43:42 - INFO - ✅ Бот 'Студия Мощь' запущен!
2026-01-02 21:44:11 - INFO - User 7556588948 started bot
2026-01-02 21:44:14 - INFO - User 7556588948 started booking
```

### Запуск админ-панели (в отдельном окне CMD):

```bash
python admin_bot/main.py --config configs/client_lite.json
```

**Логи в консоли:**
```
2026-01-02 21:44:06 - INFO - 🚀 Admin Bot started!
2026-01-02 21:46:49 - INFO - Update handled. Duration 280 ms
```

### Важно:
- Оба бота работают ОДНОВРЕМЕННО
- Общая БД, общий конфиг
- При закрытии CMD → бот останавливается
- Для фонового запуска (на сервере): `nohup python main.py &`

---

## 🎯 ТЕКУЩАЯ СТАДИЯ ПРОЕКТА

**Версия:** 2.7.0  
**Дата:** 02.01.2026  
**Статус:** 🟡 Финальная стадия, исправление багов перед запуском продаж

### ✅ Что УЖЕ работает:

**Клиентский бот:**
- ✅ FSM бронирования (10 шагов)
- ✅ Выбор услуги, мастера, даты, времени
- ✅ Ввод имени, телефона, комментария
- ✅ Подтверждение записи
- ✅ Просмотр своих записей (список активных)
- ✅ Редактирование записей (услуга, дата, время)
- ✅ Отмена записей
- ✅ FAQ (часто задаваемые вопросы)
- ✅ Информация о салоне

**Админ-панель:**
- ✅ Просмотр всех заказов (сегодня, неделя, месяц)
- ✅ Фильтрация заказов (по статусу, дате, мастеру)
- ✅ Подтверждение/отмена заказов
- ✅ База клиентов (имена, телефоны, количество визитов)
- ✅ Статистика (базовая): количество записей, выручка
- ✅ Получение уведомлений (новая запись, отмена, редактирование)

**Техническое:**
- ✅ База данных SQLite (защита от race condition)
- ✅ Логирование с маскировкой PII (имена → "И***", телефоны → "8905***6685")
- ✅ 73 теста (pytest), покрытие ~70%
- ✅ Валидация данных (телефон, дата, время)
- ✅ Календарь выбора даты (inline клавиатура)

### ❌ Что НЕ работает / требует доработки:

**См. полный список:** `.docs/BUGS_TRACKER.md`

**Критичные баги (блокируют продажу):** ~3  
**Важные (неудобно, но работает):** ~7  
**Улучшения (можно потом):** ~12

### 🎯 Что планируется добавить:

**В разработке:**
- Акция 1: Промокоды (ручной ввод + реферальные ссылки)
- Акция 2: Автоскидка за запись через бот (постоянная)

**В планах (после запуска продаж):**
- Бот-рассыльщик (для поиска клиентов)
- Сайт с офертой (описание продукта, цены, контакты)
- Интеграция с платёжными системами (ЮKassa, Stripe)
- Аналитика (расширенная статистика)
- Напоминания клиентам (за 1 день до визита)

---

## 👥 РОЛИ ПОЛЬЗОВАТЕЛЕЙ

### 🙋 Клиент салона:
**Что может:**
- Записаться на услугу
- Посмотреть свои записи
- Отредактировать запись (до определённого времени)
- Отменить запись
- Прочитать FAQ
- Посмотреть контакты салона

**Что НЕ может:**
- Видеть записи других клиентов
- Видеть админ-панель
- Менять настройки бота

### 👨‍💼 Владелец салона (админ):
**Что может:**
- Всё, что клиент
- Видеть ВСЕ записи
- Подтверждать/отменять записи
- Видеть базу клиентов
- Видеть статистику
- Настраивать бота (редактировать конфиг)
- Получать уведомления о новых записях

**Что НЕ может:**
- Изменять код бота (только конфиг JSON)

### 🤖 Разработчик (AI, ты):
**Что делаешь:**
- Исправляешь баги
- Добавляешь фичи
- Пишешь тесты
- Обновляешь документацию
- Объясняешь владельцу (БЕЗ кода!)

**Чего НЕ делаешь:**
- Не меняешь бизнес-логику без согласования
- Не усложняешь код (клиенты должны понимать)
- Не показываешь код владельцу

---

## 🔐 БЕЗОПАСНОСТЬ И ПРИВАТНОСТЬ

### Маскировка PII в логах:
**Используется:** `utils/privacy.py`

**Что маскируется:**
- Имена: "Мария" → "М***"
- Телефоны: "+79051234567" → "+7905***6685"
- Адреса: "ул. Ленина, 123" → "ул. Л***, ***"

**Зачем:**
- Защита персональных данных (GDPR, 152-ФЗ)
- Безопасность при передаче логов

### Защита токенов:
**Файл `.env`:**
```
BOT_TOKEN=8504388259:AAG...
ADMIN_BOT_TOKEN=8253921159:AAH...
ADMIN_IDS=5122428712
```

**Важно:**
- `.env` в `.gitignore` (НЕ коммитится)
- При передаче проекта → создавать новый `.env`

### Проверка прав доступа:
**Админ-панель:**
- Проверка `user_id` в `ADMIN_IDS` (из `.env`)
- Если не админ → "⛔ Доступ запрещён"

---

## 📊 МЕТРИКИ ПРОЕКТА (на 02.01.2026)

**Разработка:**
- Строк кода: ~3000
- Файлов: ~30
- Тестов: 73 (покрытие ~70%)
- Время разработки: 10-14 недель
- Коммитов: ~300
- Версия: 2.7.0

**Готовность:**
- Основная функциональность: ✅ 100%
- Критичные баги: ❌ ~3 шт
- Важные баги: ❌ ~7 шт
- Готовность к продаже: 🟡 ~65%

**Планируемый запуск:** 15-20.01.2026 (через 2-3 недели)

---

## 💰 БИЗНЕС-МОДЕЛЬ

### Цена для клиентов (салонов):
- **Разовый платёж:** 5000₽ (внедрение и настройка)
- **Ежемесячная подписка:** 500₽/мес (за VPS сервер)

**Итого:** 5000₽ + 500₽×12 = 11 000₽ в первый год

### Конкуренты:
- **Yclients:** ~3000₽/мес (дорого, много лишнего)
- **Dikidi:** ~2000₽/мес (перегружено функциями)
- **Простые боты:** 0-1000₽/мес (без поддержки, глючные)

### Преимущество этого бота:
✅ Простота (только нужное, без лишнего)  
✅ Telegram (клиенты уже пользуются)  
✅ Цена (в 2-4 раза дешевле конкурентов)  
✅ Поддержка (владелец помогает при внедрении)  
✅ Гибкость (можно настроить под любой салон)

### Целевые продажи:
- **Первая цель:** 20 ботов (100 000₽ разово + 10 000₽/мес)
- **Вторая цель:** 50 ботов (250 000₽ разово + 25 000₽/мес)
- **Долгосрочная:** 100 ботов (500 000₽ разово + 50 000₽/мес)

---

## 🛠️ ИНСТРУМЕНТЫ РАЗРАБОТКИ

### Что использует владелец:
- **Claude AI Code** (лимиты восстанавливаются 4-5 часов)
- **Firebase Gemini** (иногда зависает)
- **Perplexity AI** (Claude Sonnet 4.5 thinking) — сейчас ты здесь!

### Git workflow:
- **Ветка `main`:** Стабильная версия (деплой)
- **Коммиты:** С префиксами (fix:, feat:, docs:)
- **Тэги:** v2.7.0, v2.6.0 и т.д.

### Окружение тестирования:
- **ОС:** Windows 10 (локально на компьютере владельца)
- **Python:** 3.10+
- **База данных:** SQLite (файл `db_mini_studio.sqlite`)
- **Тестовый клиент:** User ID = 7556588948

---

## 📝 СТИЛЬ КОДА

### Правила:
- **PEP 8** (Python style guide)
- **Типизация:** Type hints желательно
- **Документация:** Docstrings для функций
- **Логирование:** `logging`, НЕ `print()`
- **Простота > сложность** (клиенты должны читать код)

### Пример хорошего кода:

```python
async def create_order(
    user_id: int, 
    service_id: str, 
    booking_date: str,
    slot_time: str
) -> int:
    """
    Создать заказ в базе данных.

    Args:
        user_id: Telegram ID клиента
        service_id: ID услуги из конфига
        booking_date: Дата записи (YYYY-MM-DD)
        slot_time: Время слота (HH:MM)

    Returns:
        order_id: ID созданного заказа

    Raises:
        ValueError: Если слот уже занят
    """
    logger.info(f"Creating order for user {user_id}")

    # Простой, понятный код
    # Комментарии только для неочевидного
    ...
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Запуск тестов:

```bash
# Все тесты
pytest tests/

# Конкретный файл
pytest tests/test_booking.py

# С покрытием
pytest --cov=. tests/
```

### Что тестируется:
- ✅ БД операции (CRUD)
- ✅ Валидация данных (телефон, дата)
- ✅ FSM логика (переходы состояний)
- ✅ Календарь (генерация дат)
- ✅ Маскировка PII

### Требования:
- Все тесты должны проходить перед коммитом
- Новые функции → новые тесты
- Покрытие >70% (желательно)

---

## 🔄 DEPLOYMENT (планируется)

**Сейчас:**
- Только локальное тестирование (Windows)
- Руководство по установке: `docs/QUICKSTART.md`

**Планируется (после запуска продаж):**
- VPS на Linux (для каждого клиента отдельный сервер)
- Docker контейнеры
- Автоматический деплой через GitHub Actions
- Мониторинг (healthchecks)

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

**Для людей (владельцев салонов):**
- `README.md` — описание проекта
- `docs/QUICKSTART.md` — быстрый старт
- `docs/CHANGELOG.md` — история версий
- `docs/BUGFIXES.md` — исправленные баги
- `docs/TESTING_GUIDE.md` — как тестировать

**Для AI (тебя):**
- `AI_WORKFLOW.md` — инструкция для AI
- `.docs/CONTEXT_FOR_AI.md` — этот файл
- `.docs/PROJECT_STATE.md` — текущее состояние
- `.docs/BUGS_TRACKER.md` — трекер багов
- `.docs/CHANGELOG_AI.md` — история изменений AI

---

## ❓ FAQ ДЛЯ AI

**Q: Можно ли менять структуру проекта?**  
A: НЕТ, только после согласования с владельцем

**Q: Можно ли использовать новые библиотеки?**  
A: Только если критично необходимо. Добавляй в `requirements.txt`

**Q: Что делать, если не понимаю задачу?**  
A: Спроси уточнение у владельца СРАЗУ, не додумывай сам

**Q: Можно ли переписать код "красиво"?**  
A: НЕТ, простота важнее "красоты". Работает = не трогай

**Q: Как часто обновлять документацию?**  
A: После КАЖДОГО изменения кода (обязательно!)

**Q: Что делать при критической ошибке?**  
A: Останови работу, откати изменения, сообщи владельцу

**Q: Можно ли показывать код владельцу?**  
A: НЕТ! Только логика простыми словами

**Q: Сколько времени работать над задачей?**  
A: Критичные баги — до 1 дня. Важные — до 3 дней. Если дольше — сообщи владельцу

---

## 🎓 УЧИСЬ НА ОШИБКАХ

**Типичные ошибки предыдущих AI:**

1. **Не спросили уточнение** → сделали не то → потеряли время
2. **Изменили рабочий код** → сломали функциональность
3. **Забыли обновить документацию** → следующий AI потерялся
4. **Не запустили тесты** → закоммитили сломанный код
5. **Показали код владельцу** → он не понял, запутался

**Как избежать:**
- Читай документацию ПОЛНОСТЬЮ
- Спрашивай, не стесняйся
- Запускай тесты ВСЕГДА
- Обновляй документацию СРАЗУ
- Объясняй простыми словами

---

## 💡 ФИНАЛЬНЫЕ СОВЕТЫ

### Помни:
- Ты не один — работают Claude, Firebase, GPT. Документация — ваша связь
- Владелец устал (10-14 недель работы), будь терпелив
- Цель — продажи, а не "идеальный код"
- Клиенты — не программисты, делай просто
- Каждый исправленный баг = шаг к запуску = деньги владельцу

### Твоя работа важна!
Этот бот помогает малому бизнесу (салонам красоты) автоматизировать процессы, расти и зарабатывать больше. Спасибо за вклад! ❤️

---

**Готов к работе? Читай `.docs/PROJECT_STATE.md` и `.docs/BUGS_TRACKER.md`** 🚀

*Создано: 2026-01-02 22:32:00 UTC+5*  
*Версия документа: 1.0*
