# 🐞 BUGS & REFACTORING TRACKER

---

## ✅ ИСПРАВЛЕНО (Fixed)

- **REFACTORING: Монолитный `mybookings.py` (668 строк)**
  - **Проблема:** Слишком большой и сложный файл для управления записями.
  - **Решение:** Разделить на модули (`view.py`, `cancel.py`, `reschedule.py`) в директории `handlers/mybookings/`.
  - **Статус:** ✅ **ИСПРАВЛЕНО** (2026-01-04)

- **Отсутствие FSM Storage (Minor):**
  - **Проблема:** При перезапуске бота, FSM-состояния пользователей сбрасывались.
  - **Решение:** Внедрить персистентное FSM-хранилище. Использовать `SQLiteStorage`.
  - **Статус:** ✅ **ИСПРАВЛЕНО** (2026-01-04)

- **RACE CONDITION (Критический):**
  - **Проблема:** Два пользователя могли одновременно забронировать один и тот же временной слот.
  - **Решение:** Обернуть метод `add_order` в `utils/db/booking_queries.py` в атомарную транзакцию (`with self.connection:`).
  - **Статус:** ✅ **ИСПРАВЛЕНО** (2026-01-04)

- **REFACTORING: Монолитный `db_manager.py`**
    - **Проблема:** Класс `DBManager` смешивал управление соединением и SQL-запросы.
    - **Решение:** Разделен на `DBManager` и модули с запросами в `utils/db/`.
    - **Статус:** ✅ **ИСПРАВЛЕНО** (2026-01-03)

---

## 📝 К РАБОТЕ (To Do)

- **IMPROVEMENT: Уведомления о переносе**
  - **Проблема:** Админы не получают четкого уведомления, какая именно информация была изменена в записи (старая и новая дата/время).
  - **Решение:** Доработать `send_order_change_to_admins`, чтобы в уведомлении наглядно показывались старые и новые данные.
  - **Статус:** ⏳ **В ОЖИДАНИИ**
