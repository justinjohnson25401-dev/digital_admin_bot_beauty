# 🐞 BUGS TRACKER

Здесь мы отслеживаем все баги и задачи по рефакторингу.

---

## 🐞 НЕИСПРАВЛЕННЫЕ БАГИ (TODO)

### 🔴 CRITICAL (Блокируют функционал)

---

### 🟠 MAJOR (Существенно влияют на UX)

---

### 🟡 MINOR (Незначительные проблемы)

---

## 🔄 ЗАДАЧИ ПО РЕФАКТОРИНГУ (REFACTORING)

### 1. Монолитный `db_manager.py` (772 строки)

**Проблема:**  
Слишком большой и сложный файл. Содержит логику для пользователей, заказов, мастеров, уведомлений.

**Решение:**
- Разделить на модули по ответственности:
  - `db/user_queries.py`
  - `db/order_queries.py`
  - `db/master_queries.py`
  - `db/base.py` (инициализация)

---

### 2. Монолитный `mybookings.py` (668 строк)

**Проблема:**  
Содержит всю логику управления записями пользователя (просмотр, отмена, перенос).

**Решение:**
- Разделить на модули:
  - `mybookings/view.py` (просмотр)
  - `mybookings/cancel.py` (отмена)
  - `mybookings/reschedule.py` (перенос)
  - `mybookings/keyboards.py`

---

## ✅ ИСПРАВЛЕННЫЕ БАГИ

### 🔄 2026-01-04 (Исправление FSM Storage)

#### ✅ Отсутствие FSM Storage

**Статус:** ✅ ИСПРАВЛЕНО  
**Описание:**
При перезапуске бота все активные FSM-сессии сбрасывались. Пользователям приходилось начинать заново.

**Файл:** `main.py`

**Решение:**
- `MemoryStorage()` заменено на `SQLiteStorage`.
- Хранилище теперь использует файл `fsm_data_{business_slug}.db` для сохранения состояний, что обеспечивает их персистентность между перезапусками.

---

### 🔄 2026-01-03 (Исправление Race Condition)

#### ✅ Race Condition при бронировании

**Статус:** ✅ ИСПРАВЛЕНО  
**Описание:**
Если два пользователя одновременно выбирали один и тот же слот, система могла некорректно обработать бронирование, позволив обоим занять одно время.

**Файл:** `utils/db/booking_queries.py` (метод `add_order`)

**Решение:**
- Метод `add_order` обернут в атомарную транзакцию (`with self.connection:`).

---

### 🔄 2026-01-03 03:00:00 UTC+5 (Рефакторинг модуля персонала)

#### ✅ Рефакторинг admin_handlers/staff_editor.py (1478 → модульная структура)
**Статус:** ✅ ИСПРАВЛЕНО  
**Файлы:** 
- Создано: `admin_handlers/staff/` (7 новых модулей)
- Удалено: `admin_handlers/staff_editor.py` (1478 строк)

---

#### ✅ Рефакторинг handlers/booking.py (1020 → модульная структура)
**Статус:** ✅ ИСПРАВЛЕНО  
**Файлы:**
- Создано: `handlers/booking/` (9 новых модулей)
- Удалено: `handlers/booking.py` (1020 строк)
