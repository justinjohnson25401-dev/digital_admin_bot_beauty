# 🤖 ИНСТРУКЦИЯ ДЛЯ AI АССИСТЕНТОВ

> Этот файл содержит правила работы с проектом для всех AI (Claude, GPT, Gemini, Perplexity и др.)

---

## ⚡ БЫСТРЫЙ СТАРТ

1.  **Прочитай сначала:** `.docs/CONTEXT_FOR_AI.md` — там весь контекст проекта
2.  **Проверь задачи:** `.docs/BUGS_TRACKER.md` — что нужно исправить
3.  **Узнай текущее состояние:** `.docs/PROJECT_STATE.md` — где мы находимся

---

## 🔄 WORKFLOW (пошагово)

### Шаг 1: Начало работы
```
1. Читаю AI_WORKFLOW.md (этот файл)
2. Читаю .docs/CONTEXT_FOR_AI.md (контекст)
3. Читаю .docs/PROJECT_STATE.md (текущее состояние)
4. Читаю .docs/BUGS_TRACKER.md (задачи)
5. Готов к работе!
```

### Шаг 2: Получил задачу от владельца
```
1. Понял задачу (спросил уточнения, если нужно)
2. Проверил, нет ли конфликтов с текущим состоянием
3. Зафиксировал задачу в BUGS_TRACKER.md (если её там нет)
4. Начал работу
```

### Шаг 3: Вношу изменения и тестирую
```
1. Изменяю код, следуя принципам проекта
2. Запускаю тесты, если среда позволяет (`pytest tests/` или `python run_tests.py`)
3. Проверяю логи: нет ли новых ошибок
```

### Шаг 4: Обновляю документацию
```
1. Обновляю PROJECT_STATE.md:
   - Дата/время
   - Секция "ПОСЛЕДНЕЕ ИЗМЕНЕНИЕ"
   - Статус багов и задач
   - Список изменённых файлов

2. Обновляю BUGS_TRACKER.md:
   - Отмечаю ✅ исправленное
   - Добавляю новое, если нашёл

3. Добавляю запись в CHANGELOG_AI.md:
   - Дата/время до секунды (UTC+5)
   - Контекст диалога
   - Список изменений
   - Результат проверки
```

### Шаг 5: Фиксирую изменения в Git (ОЧЕНЬ ВАЖНО)
> Этот шаг выполняется ассистентом, у которого есть доступ к `git`.
```
# 1. Добавить ВСЕ измененные файлы (код + документация)
git add .

# 2. Создать коммит с ПРАВИЛЬНЫМ сообщением
# (см. секцию "ФОРМАТ КОММИТОВ")
git commit -m "refactor: обновлена система конфигурации"

# 3. (Опционально) Отправить изменения в удаленный репозиторий
git push
```

### Шаг 6: Сообщаю владельцу
```
Формат ответа владельцу (НЕ показываю код!):

✅ Задача выполнена

**Что было сделано:**
- [описание простыми словами]

**Что изменилось:**
- [файл] → [что изменил]

**Что нужно проверить:**
- [инструкция для владельца]

**Статус:** ✅ Готово / ⚠️ Требует проверки / ❌ Проблема
```

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА

### После КАЖДОГО изменения кода:

1.  **Обнови `.docs/PROJECT_STATE.md`:**
    *   Секция "Последнее изменение"
    *   Секция "Текущий статус файлов"
    *   Дата и время (Екатеринбург UTC+5)

2.  **Обнови `.docs/BUGS_TRACKER.md`:**
    *   Если исправил баг → отметь ✅
    *   Если нашёл новый → добавь в список
    *   Перенеси исправленное в раздел "Готово"

3.  **Запиши в `.docs/CHANGELOG_AI.md`:**
    ```
    ## [YYYY-MM-DD HH:MM:SS UTC+5] Сессия #N
    **AI:** [твоё имя, например Claude 4.5]
    **Задача:** [что делал]
    **Изменения:**
    - Файл `X` → [что изменил]
    - Файл `Y` → [что изменил]
    **Результат:** ✅ Работает / ⚠️ Требует проверки / ❌ Не получилось
    **Коммит:** [хеш коммита, если есть]
    ```
---

## 🎯 СТИЛЬ РАБОТЫ С ПРОЕКТОМ

### ✅ ДЕЛАЙ:
- Читай существующий код перед изменениями
- Сохраняй стиль кодирования проекта (PEP 8)
- Добавляй логирование для отладки
- Пиши комментарии к сложной логике
- Запускай тесты после изменений: `pytest tests/`
- Проверяй, что не сломал существующую функциональность
- Пиши простой код (клиенты должны понимать)

### ❌ НЕ ДЕЛАЙ:
- НЕ переписывай весь код "с нуля"
- НЕ меняй структуру проекта без согласования
- НЕ удаляй существующие функции (только помечай @deprecated)
- НЕ коммить без описания изменений
- НЕ забывай обновлять документацию
- НЕ показывай владельцу код (он НЕ программист)

---

## ⚠️ Ограничения среды исполнения Gemini API

Эта среда является ограниченной "песочницей" и имеет следующие технические ограничения:

-   ❌ **Нет `pip`:** Установка любых Python-пакетов (`pip install ...`) невозможна.
-   ❌ **Нет `pytest`:** Запуск тестов, использующих `pytest`, невозможен.
-   ❌ **Нет `git`:** Выполнение команд `git` (commit, push, pull) невозможно.
-   ❌ **Нет системных утилит:** Команды вроде `apt-get`, `yum`, `apk` недоступны.

### Что из этого следует:

-   ✅ **Работа с кодом:** Можно читать, изменять, создавать и удалять файлы проекта.
-   ✅ **Рефакторинг:** Можно проводить рефакторинг кода, но только в пределах уже установленных зависимостей.
-   ✅ **Запуск `unittest`:** Можно запускать тесты, написанные на встроенном в Python модуле `unittest`, если они не требуют внешних зависимостей.

**Важно:** Не пытайтесь выполнять команды, которые недоступны в этой среде. Если для задачи требуются новые библиотеки или команды, сообщите об этом владельцу.

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАВЕРШЕНИЕМ РАБОТЫ

**Код:**
- [ ] Код работает (проверил локально/логически)
- [ ] Тесты проходят (если была возможность запустить)
- [ ] Логи чистые (нет ERROR/CRITICAL)
- [ ] Стиль кода соответствует PEP 8

**Документация:**
- [ ] Обновлен `PROJECT_STATE.md`
- [ ] Обновлен `BUGS_TRACKER.md`
- [ ] Записано в `CHANGELOG_AI.md`

**Коммуникация:**
- [ ] Подготовлен отчет для владельца (без кода!)
- [ ] Сформулированы инструкции для проверки

**Git (для ассистента с доступом):**
- [ ] Все изменения (код + документы) добавлены в коммит
- [ ] Написано понятное сообщение коммита

---
## 📂 СТРУКТУРА ДОКУМЕНТАЦИИ

... (остальное содержимое файла без изменений)
