# ✅ Проверка после исправлений (быстрый чеклист)

## 1) Подготовка

- Убедись, что в корне проекта нет лишних кэшей: `__pycache__` (не обязательно, но ок)
- Убедись, что существует `.env` и в нем есть:
  - `BOT_TOKEN`
  - `ADMIN_BOT_TOKEN`

## 2) Важно: удалить дубликат admin_handlers

В проекте должны использоваться **только** обработчики из папки:
- `bot_business_v2_final/admin_handlers/`

Если есть папка:
- `bot_business_v2_final/admin_bot/admin_handlers/`

то её нужно **удалить**, чтобы не было путаницы.

## 3) Запуск (локально)

### Client bot

Запусти в терминале 1:
```bash
python main.py --config configs/client_lite.json
```
Ожидаемо в логах:
- `Database schema version: 2`
- `Started polling`

### Admin bot

Запусти в терминале 2:
```bash
python admin_bot/main.py --config configs/client_lite.json
```
Ожидаемо в логах:
- `Started polling`

## 4) Проверка клиентского бота (критичные сценарии)

- `/start` открывает меню
- `📅 Записаться / Заказать`
  - если список услуг пустой — выводится понятное сообщение
  - нельзя выбрать прошедшую дату
  - нельзя выбрать прошедшее время (для "сегодня")
- Ввод имени:
  - меньше 2 символов — ошибка
  - больше 100 символов — ошибка
- Ввод комментария:
  - больше 500 символов — ошибка
- На экране подтверждения:
  - двойной клик по "✅ Да, подтвердить" не создает дубль (пишет "Уже обработано")
- Race condition:
  - если слот занял другой пользователь до подтверждения — бот просит выбрать другое время, без падения

## 5) Проверка админ-бота

- `/start`:
  - если PIN включен — просит PIN
  - после верного PIN показывает главное меню с кнопками
- Ввод неверного PIN:
  - для владельца (ID из `admin_ids`) **нет блокировки**
  - для остальных работает блокировка/лимиты

## 6) Логи

- Файлы:
  - `logs/client_bot.log`
  - `logs/admin_bot.log`

Если логов нет — проверь права/папку `logs/`.
